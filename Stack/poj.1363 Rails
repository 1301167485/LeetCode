1.题目描述：已知从1到n的数字序列，按顺序入栈，每个数字入栈后即可出栈，也可以停留在栈中，等待后面的数字入栈出栈后，该数组字再出栈，求该数字序列的出栈序列是否合法？

  eg ：
    3、2、5、4、1  合法
    3、1、2、4、5  不合法，因为1比2先出栈
  
2.思路：模拟
  1）拿3、2、5、4、1距离
  2）定义一个栈stack
  2）定义一个辅助队列queue，按顺序存储1到n的数字，如上例中queue = 1，2，3，4，5
  3）按queue的顺序，不断将元素push进栈stack中，每次判断栈顶元素与给定的数字序列的第一个元素是否相等
  4）如果相等，则数字序列向后移动一位，且栈弹出一个元素，不相等则队列继续入栈直到相等。
  5）遍历完以后看是否栈为空，是则返回true，否则返回false
  
  初始：queue = 1，2，3，4，5  stack = null array = 3,2,5,4,1
  1）queue第一个元素1进栈，
  2）判断栈首元素（1）与array第一个元素（3）是否相等
  3）不相等，queue下一个元素2进栈
  4）判断栈首元素（2）与array当前元素是否相等
  5）不相等，queue下一个元素3进栈
  6）判断栈首元素（3）与array当前元素是否相等
  7）相等，stack栈顶元素出栈，array向后移动一位为2
  8）判断栈首元素（2）与array当前元素是否相等
  9）相等，stack栈顶元素出栈，array向后移动以为为5
  10）判断栈首元素（1）与array当前元素是否相等
  11）不相等，queue下一个元素4进栈，
  12）继续判断
  13）不相等，queue下一个元素5进栈，
  14）栈顶元素5与array当前元素相等，栈顶元素出栈，array向后移动一位
  15）栈顶元素4与array当前元素相等，栈顶元素出栈，array向后移动一位
  16）栈顶元素41与array当前元素相等，栈顶元素出栈
  17）此时栈为空，且已经遍历完。返回true
